# 破局 FDE 检索记录（2026-09-16）

## 检索范围

用户要求将 FDE 官网主题中点赞最多的前 25 篇分类整理并发布到本仓库。查询使用用户指定的破局 CLI，不对主题执行点赞、收藏、评论等写入。

先用 `breakout capabilities --json` 检查查询 Schema。搜索命令为 `breakout topic list --keyword FDE --size 100 --page N --json`，N 为 1 至 13；详情命令为 `breakout topic detail TOPIC_ID --json`。单次最多两个并发请求。

13 页均返回 total=1255；去重后共 1255 个 topicId。搜索默认顺序并非时间或点赞顺序，因此在本地排序。去除检索正文摘要中的 HTML 标签与 URL，再用独立单词 FDE 或 Forward Deployed 匹配标题和可见摘要，得到 225 个候选。排除只在链接里命中 fde 的 AI 写真、绘画等结果。额外检查总赞不少于 110、正文摘要未匹配但 AI 摘要提到 FDE 的结果：未发现此类遗漏。

## 排名口径

采用列表查询时的 totalLikeCount；保留 likeCount 和 systemLikeCount，并验证总数等于二者之和。并列时按创建时间倒序、topicId 字符串顺序处理。第 16 与 17 名均为 151 赞，第 20 与 21 名均为 116 赞。第 25 名为 110 赞，第 26 名为 108 赞。列表与详情查询分时执行，不用详情动态点赞数替换列表快照。

未加日期过滤，保留 2026-03-24 的新人导航主题。24 篇创建于七月至九月；该旧帖的当前正文包含后续活动，因此创建时间不代表正文最后更新。排名时段和内容性质均在目录中说明。

## 阅读与权限边界

25 个官网详情全部读取；部分内容是飞书长文导读，外链全文没有读取，AI 摘要不视为已核验项目事实。公开仓库不存放 CLI 返回的完整正文、会员材料、提示词库、头像或当前用户互动状态，只提交原创梳理和排名元数据。查询临时原始数据留在本机临时目录，没有删除。

官网主题按小时同步，最新内容可能延迟约 60 分钟。CLI 时间已统一为上海时间，本次不再额外加减八小时。作者优先使用 fullName，与官网查询返回的 userFullName 可能不同，未主动合并身份。

## 分类产物

- 角色认知与职业路径：4 篇。
- Skill、Agent与系统化：7 篇。
- 商业定位、获客与选单：3 篇。
- 企业实战与交付观察：5 篇。
- 社区生态与学习资源：6 篇。

[分类梳理](../knowledge-base/07-中国社区实践/01-破局FDE点赞Top25分类梳理.md)提供全部 25 篇的排名、原创摘要、工作包与资料边界；[元数据快照](poju-fde-top25-2026-09-16.json)用于核对数字。完整原始检索页面未公开，在线数据变化后重新检索不保证还原当时全量顺序。
